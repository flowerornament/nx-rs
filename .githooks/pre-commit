#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "${ROOT_DIR}"

if [[ ! -f "scripts/agent-hooks/pre-compile.sh" ]]; then
  echo "[pre-commit] missing scripts/agent-hooks/pre-compile.sh"
  exit 1
fi

scripts/agent-hooks/pre-compile.sh

