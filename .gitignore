target/

# bd sync-branch artifacts are tracked in beads-sync, not main.
.beads/issues.jsonl
.beads/interactions.jsonl
.beads/metadata.json

# Python reference cache artifacts (generated when parity invokes nx-python).
reference/nx-python/**/__pycache__/
reference/nx-python/**/*.pyc
